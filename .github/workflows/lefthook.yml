name: "lefthook"
on:
  push:
    paths:
      - ".github/workflows/lefthook.yml"
      - "lib/**"
      - "src/lefthook/**"
      - "test/lefthook/**"
    tags:
  pull_request:
    paths:
      - ".github/workflows/lefthook.yml"
      - "lib/**"
      - "src/lefthook/**"
      - "test/lefthook/**"

permissions:
  contents: write
  packages: write

jobs:
  lefthook:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - run: npm install -g @devcontainers/cli
        name: Install devcontainer CLI

      - run: devcontainer features test --skip-autogenerated -f lefthook
        name: Run tests

      - run: echo "version=${GITHUB_REF_NAME#lefthook-v}" >> "$GITHUB_OUTPUT"
        name: Extract release version
        id: release_version
        if: startsWith(github.ref, 'refs/tags/lefthook-v')

      - uses: mindsers/changelog-reader-action@v2
        id: changelog_reader
        if: startsWith(github.ref, 'refs/tags/lefthook-v')
        with:
          path: ./src/lefthook/CHANGELOG.md
          version: ${{ steps.release_version.outputs.version }}

      - run: devcontainer features publish -n nicoviii/devcontainer-features ./src/lefthook
        name: Publish feature
        if: startsWith(github.ref, 'refs/tags/lefthook-v')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: softprops/action-gh-release@v3
        if: startsWith(github.ref, 'refs/tags/lefthook-v')
        with:
          body: ${{ steps.changelog_reader.outputs.changes }}
          prerelease: ${{ steps.changelog_reader.outputs.status == 'unreleased' }}
