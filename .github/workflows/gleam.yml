name: "gleam"
on:
  push:
    paths:
      - ".github/workflows/gleam.yml"
      - "lib/**"
      - "src/gleam/**"
      - "test/gleam/**"
    tags:
  pull_request:
    paths:
      - ".github/workflows/gleam.yml"
      - "lib/**"
      - "src/gleam/**"
      - "test/gleam/**"

permissions:
  contents: write
  packages: write

jobs:
  gleam:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - run: npm install -g @devcontainers/cli
        name: Install devcontainer CLI

      - run: devcontainer features test --skip-autogenerated -f gleam
        name: Run tests

      - run: echo "version=${GITHUB_REF_NAME#gleam-v}" >> "$GITHUB_OUTPUT"
        name: Extract release version
        id: release_version
        if: startsWith(github.ref, 'refs/tags/gleam-v')

      - uses: mindsers/changelog-reader-action@v2
        id: changelog_reader
        if: startsWith(github.ref, 'refs/tags/gleam-v')
        with:
          path: ./src/gleam/CHANGELOG.md
          version: ${{ steps.release_version.outputs.version }}

      - run: devcontainer features publish -n nicoviii/devcontainer-features ./src/gleam
        name: Publish feature
        if: startsWith(github.ref, 'refs/tags/gleam-v')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/gleam-v')
        with:
          body: ${{ steps.changelog_reader.outputs.changes }}
          prerelease: ${{ steps.changelog_reader.outputs.status == 'unreleased' }}
