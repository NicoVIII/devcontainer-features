name: "amber"
on:
  push:
    paths:
      - ".github/workflows/amber.yml"
      - "lib/**"
      - "src/amber/**"
      - "test/amber/**"
    tags:
  pull_request:
    paths:
      - ".github/workflows/amber.yml"
      - "lib/**"
      - "src/amber/**"
      - "test/amber/**"

permissions:
  contents: write
  packages: write

jobs:
  amber:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - run: npm install -g @devcontainers/cli
        name: Install devcontainer CLI

      - run: devcontainer features test --skip-autogenerated -f amber
        name: Run tests

      - run: echo "version=${GITHUB_REF_NAME#amber-v}" >> "$GITHUB_OUTPUT"
        name: Extract release version
        id: release_version
        if: startsWith(github.ref, 'refs/tags/amber-v')

      - uses: mindsers/changelog-reader-action@v2
        id: changelog_reader
        if: startsWith(github.ref, 'refs/tags/amber-v')
        with:
          path: ./src/amber/CHANGELOG.md
          version: ${{ steps.release_version.outputs.version }}

      - run: devcontainer features publish -n nicoviii/devcontainer-features ./src/amber
        name: Publish feature
        if: startsWith(github.ref, 'refs/tags/amber-v')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/amber-v')
        with:
          body: ${{ steps.changelog_reader.outputs.changes }}
          prerelease: ${{ steps.changelog_reader.outputs.status == 'unreleased' }}
