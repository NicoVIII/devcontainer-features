name: "just"
on:
  push:
    paths:
      - ".github/workflows/just.yml"
      - "lib/**"
      - "src/just/**"
      - "test/just/**"
    tags:
  pull_request:
    paths:
      - ".github/workflows/just.yml"
      - "lib/**"
      - "src/just/**"
      - "test/just/**"

permissions:
  contents: write
  packages: write

jobs:
  just:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - run: npm install -g @devcontainers/cli
        name: Install devcontainer CLI

      - run: devcontainer features test --skip-autogenerated -f just
        name: Run tests

      - run: echo "version=${GITHUB_REF_NAME#just-v}" >> "$GITHUB_OUTPUT"
        name: Extract release version
        id: release_version
        if: startsWith(github.ref, 'refs/tags/just-v')

      - uses: mindsers/changelog-reader-action@v2
        id: changelog_reader
        if: startsWith(github.ref, 'refs/tags/just-v')
        with:
          path: ./src/just/CHANGELOG.md
          version: ${{ steps.release_version.outputs.version }}

      - run: devcontainer features publish -n nicoviii/devcontainer-features ./src/just
        name: Publish feature
        if: startsWith(github.ref, 'refs/tags/just-v')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: softprops/action-gh-release@v3
        if: startsWith(github.ref, 'refs/tags/just-v')
        with:
          body: ${{ steps.changelog_reader.outputs.changes }}
          prerelease: ${{ steps.changelog_reader.outputs.status == 'unreleased' }}
